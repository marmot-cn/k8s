apiVersion: extensions/v1beta1
kind: HorizontalPodAutoscaler
metadata:
  name: nginx-deployment
spec:
  scaleRef:
    kind: Deployment
    name: nginx-deployment
    subresource: scale
  minReplicas: 1
  maxReplicas: 2
  cpuUtilization:
    targetPercentage: 50
